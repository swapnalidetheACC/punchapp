{
  "name": "Punchapp",
  "version": "1.0.0",
  "scripts": {
    "install-client": "cd client && npm install",
    "build": "cd client && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "couchbase": "^4.3.0",
    "dotenv": "^16.4.5",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "20.x"
  }
}
